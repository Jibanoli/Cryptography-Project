# SecureSign PKI 2FA Tool
This tool provides **Multi-Factor Authentication** using:
1. Knowledge (Password)
2. Possession (Email OTP)
3. [cite_start]Cryptographic Identity (RSA Digital Signatures) [cite: 31]

## How to Run
1. Install dependencies: `pip install -r requirements.txt`
2. Run the server: `python app.py`
